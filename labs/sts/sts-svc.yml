# sts/sts-lb.yml
apiVersion: v1
kind: Service
metadata:
    name: nodesjs-sfs-svc
spec:
    # no sitcky session
    sessionAffinity: None
    ports:
    - port: 80
      targetPort: 8080
    selector:
        app: nodejs-sfs